.Wrapper
  %br/
  .field
    .item
      %P.item__name 
        = @item.name
      .item__image
        .item__image__first
          = image_tag "#{@item.item_images[0].image}"
        %ul
          - @item.item_images.each do |image|
            = image_tag "#{image.image}"
        %br/
        .item__price
          = "¥#{@item.price.to_s(:delimited)}"
          .item__price__sub
            (税込) 送料込み
        %br/
      -if !current_user || current_user.id != @item.saler_id
        .item__btn
          = link_to "#"  do
            .item__btn__buy
              購入画面に進む
      -if user_signed_in? && current_user.id == @item.saler_id
        .item__btn
          = link_to edit_item_path  do
            .item__btn__edit
              編集する
        .item__btn
          = link_to @item, method: :delete, data: { confirm: '商品を削除しますか？' }  do
            .item__btn__delete
              削除する
      .item__description
        = @item.description
        %br/
      .item__info
        %table
          %tbody
            %tr
              %th 出品者
              %td= @item.saler.nickname
            %tr 
              %th カテゴリー    
              %td
                -if @item.category.parent != nil
                  = link_to "#{@item.category.parent.parent.name}"
                  %br/
                  = link_to "#{@item.category.parent.name}" 
                  %br/
                = link_to "#{@item.category.name}"
            %tr
              %th ブランド
              %td 
                -if @item.brand != ""
                  = @item.brand
                -else 
                  ブランドの指定はありません
            %tr
              %th 商品サイズ
              %td サイズ指定はありません
            %tr
              %th 商品の状態
              %td= @item.condition_id
            %tr
              %th 配送料の負担
              %td 
                = @item.delivery_fee_id
            %tr
              %th 発送元の地域
              %td
            %tr
              %th 発送日の目安
              %td= @item.id
      .check_btn
        .check_btn__like
          =link_to "#" do
            = icon( "fa", "star")
            お気に入り 1
        .check_btn__bad
          =link_to "#" do
            = icon("fa", "flag")
            不適切な商品の通報
    %br/
  %br/
  .field
    .comment
      .comment__box
        %textarea
      .comment__message 
        相手のことを考え丁寧なコメントを心がけましょう。
        %br/
        不快な言葉遣いなどは利用制限や退会処分となることがあります。
        %br/
      .comment__submit{ type: "submit"}
        = link_to "#" do
          .comment__submit__btn
            = icon("fa","comment")
            コメントする
= render "show_link"
= render "another_item"
